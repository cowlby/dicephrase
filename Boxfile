web1:
    # Name
    name: dicephrase-web

    # General settings
    shared_writable_dirs:
        - /cache
        - /logs

    # Web settings
    document_root: /web
    default_gateway: index.php
    index_list: [index.php]

    # PHP Settings
    php_display_errors: "1"
    php_session_autostart: Off
    php_short_open_tag: Off
    magic_quotes_gpc: Off
    php_extensions:
        - curl

    #newrelic
    php_newrelic_framework: symfony

    # Hooks
    after_build:
        - "bash bin/install_composer.sh"
        - "bash bin/create_htaccess.sh"

db1:
  name: dicephrase-db
  type: mysql

cache1:
  name: dicephrase-cache
  type: memcached
